-- script that creates a table called first_table in the current
-- MySQL server, database name will be passed as argument of mysql command
CREATE TABLE IF NOT EXISTS first_table (id INT, name VARCHAR(256));
